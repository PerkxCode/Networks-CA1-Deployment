# Use the official, lightweight Nginx web server image
FROM nginx:alpine

# Copy our custom HTML file into the default Nginx hosting directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 to allow web traffic
EXPOSE 80